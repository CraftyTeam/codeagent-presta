# CodeAgent Presta Updates

Distribution-only repository for the CodeAgent PrestaShop module.

Canonical source code lives in the private `code.codeagent.ro` monorepo under `clients/prestashop-module/`.

`update.json` and GitHub Release assets remain here for installed-client updates.
